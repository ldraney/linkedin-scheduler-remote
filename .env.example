# LinkedIn OAuth (from your LinkedIn Developer App)
LINKEDIN_OAUTH_CLIENT_ID=your_client_id
LINKEDIN_OAUTH_CLIENT_SECRET=your_client_secret

# Server
HOST=127.0.0.1
PORT=8002
BASE_URL=https://your-public-hostname.example.com

# Session secret (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SESSION_SECRET=your_random_secret
